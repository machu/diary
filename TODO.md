# TODO

最終更新: 2026-07-20

Astro 7.1.1 へのアップグレード後に実施する作業。互換レイヤーの撤去と表示変更は、それぞれ独立したPRで扱う。

## 優先度: 高

### Vercelの画像ビルド時間を確認する

- [x] 同一ブランチの連続したデプロイで、画像処理フェーズの所要時間を比較する
- [x] VercelのログでBuild Cacheが復元されていることを確認する
- [x] `VERCEL_FORCE_NO_BUILD_CACHE=1`が設定されていないことを確認する
- [x] キャッシュ利用後も遅い場合、Astro画像キャッシュの容量を記録する
- [x] Vercelの1GB上限を超える場合、生成サイズ数の削減、画像の`public/`移動、外部画像サービスの利用を比較する

完了条件: キャッシュあり・なしのビルド時間と画像処理時間を記録し、現在の設定を維持するか改善作業を起票できること。

実施結果 (2026-07-20):

- Vercelビルドはキャッシュなしで8分59秒、Build Cache復元後は1分46秒。7分13秒 (約80%) 短縮した。
- VercelログでBuild Cacheの復元と利用を確認。画像処理単体の所要時間はログから分離できないため、ビルド全体で比較した。
- キャッシュ利用後は十分に高速で、表示にも問題なし。キャッシュ容量の追加調査と1GB上限対策は不要と判断し、現在の設定を維持する。

### PR #6の要件をAstro 7へ移植する

- [x] PR #6「全文検索ページ + 関連記事コンポーネント」の実現性と代替案を再評価する
- [x] 現行の`entry.id`とContent Layer APIに合わせて実装する
- [x] 検索結果と関連記事のURLを`/posts/YYYYMMDD/pNN`に統一する
- [x] 全文検索、ランキング、ビルド成果物、主要ブラウザ導線のテストを追加する
- [x] `pnpm verify`を実行する

完了条件: PR #6の要件がAstro 7環境へ移植され、全文検索と関連記事が本番ビルドで検証できること。

実施結果 (2026-07-20):

- PR #6をそのままrebaseせず、現在のContent Layer APIとURLヘルパーを使う新規実装に置き換えた。
- PagefindをAstroビルド後に実行し、本番の投稿詳細1,507ページ、24,991語、タグフィルター1種を静的索引化した。旧URLのリダイレクトHTMLは索引対象外とした。
- 検索ページとPC・モバイルのヘッダー検索を追加し、検索語を`?q=`で共有可能にした。タイトル、日付、タグ、本文抜粋を表示する。
- `pnpm build`で生成した`dist/pagefind`を開発サーバーの`/pagefind/`から配信し、本番と同じ検索を`pnpm dev`でも確認可能にした。
- 関連記事はタグに依存させず、タイトルと本文を`Intl.Segmenter`で日本語単語に分割し、一般語を除いたTF-IDFコサイン類似度、日付の近さ、新しさの順で最大3件を選ぶ。全投稿の候補は`getStaticPaths()`で一度だけ計算する。
- `pnpm verify`が成功。ユニット22件、成果物12件、PreviewのChromium E2E 6件、DevのChromium E2E 2件で検索、タグURL、本文類似度、候補数、同日除外、モバイル検索を確認した。

## 優先度: 中

### Markdown処理系をSätteriへ移行する

- [x] 新旧記事、コードブロック、埋め込みHTMLを含む代表ページを選ぶ
- [x] 現行のunified/remark出力を比較用に保存する
- [x] `markdown.processor: unified()`を外す
- [x] 代表ページのHTMLと表示を比較し、非互換箇所を修正する
- [x] 問題がなければ`@astrojs/markdown-remark`を削除する
- [x] ビルド時間と生成物サイズの変化を記録する

完了条件: 代表記事に意図しない表示差分がなく、全検証コマンドが成功すること。影響範囲が大きい場合はunifiedを維持し、理由を記録する。

実施結果 (2026-07-20):

- 代表記事: `20031225p01` (最古・コードブロック)、`20140818p01` (中期・コードブロック)、`20220305p01` (埋め込みHTML)、`20250831p01` (最新・言語指定コードブロック)。
- unified/remarkとSätteriの出力を全1,507記事でDOM比較し、リンク構造が崩れた旧記事2件のMarkdownを修正。修正後の構造差分は0件。HTMLエンティティの表記とSätteriのスマート句読点によるダッシュ・引用符の変化は新プロセッサの意図した差分として確認。
- 実ブラウザで代表記事を確認し、コードブロック、Twitter埋め込みHTML、修正したリンクが表示され、水平オーバーフローとブラウザエラーがないことを確認。
- 単発のローカル計測 (画像キャッシュ利用): ビルドは22.99秒から21.21秒、`dist/`は453,124 KiBから453,128 KiB。生成物はどちらも6,350ファイル、2,789ページ。

### Astro 7のJSX空白規則へ移行する

- [x] インライン要素、タグバッジ、前後リンクを含む代表ページを選ぶ
- [x] `compressHTML: true`を外す
- [ ] HTML差分とPC・モバイル表示を確認する
- [x] 意図しない空白の欠落や追加を修正する
- [x] ビルド時間と生成物サイズの変化を記録する

完了条件: 主要ページのテキストとレイアウトに意図しない差分がなく、全検証コマンドが成功すること。

実施状況 (2026-07-20):

- 代表ページ: `/` (カード、タグバッジ、ページ送り)、`/tags/` (インラインの件数)、`/years/2025/` (月見出しと件数)、`/posts/20220305/p01` (記事タグと前後リンク)。
- `compressHTML: true`を削除し、生成した全3,885 HTMLを旧出力と比較。要素構造、属性、非空白テキストに差分はなく、差分はAstroが要素間へ挿入していた空白文字の除去のみ。インライン要素間の見た目は既存の`gap-*`または`ml-*`で明示されているため、テンプレートの追加修正は不要だった。
- 単発のローカル計測 (画像キャッシュ利用): ビルドは22.92秒から21.97秒、`dist/`は453,128 KiBから452,864 KiB。生成物はどちらも6,350ファイル、2,789ページ。
- `pnpm test`、`pnpm lint`、`pnpm build`、`pnpm test:output`は成功。実行環境の権限制限でプレビューサーバーを起動できなかったため、PC・モバイルのブラウザ目視と`pnpm test:e2e`は未実施。

## 優先度: 低

### 出力検証を自動化する

- [x] `dist/**/*.html`を対象にしたビルド出力テストを追加する
- [x] 日付表記、タグURL、ページネーション、旧URLの転送先と301ルールを検証する
- [x] 必要最小限のPlaywright E2Eを追加する
- [ ] Vercel Previewで旧URLが実際に301と正しい`Location`を返すことを確認する

実装結果 (2026-07-20):

- Vitestの成果物テストを通常のユニットテストから分離し、全HTML、日付、タグ、ページネーション、旧URLのリダイレクトHTMLを検証。
- `vercel.mjs`で公開対象のMarkdownからVercel redirectsを動的生成し、全ルールの一意性、転送先、`statusCode: 301`を検証。Hobbyプランで利用できないBulk Redirectsは採用しない。
- ChromiumのみのPlaywright E2Eで、トップから記事、ページ送り、タグ遷移、旧URLの単一記事日・複数記事日を検証。
- `pnpm verify`とGitHub Actionsを追加。Vercel PreviewのHTTP確認は`pnpm test:redirects -- <preview-url>`で実行する。
- PR #9のVercel Previewはデプロイに成功し、ログイン済みブラウザで`/diary/`、単一記事日、複数記事日の転送先が一致することを確認。Deployment Protectionが未認証HTTP要求を302でログインへ転送するため、301と`Location`の直接確認は保護解除またはAutomation Bypass設定後に実施する。

### 周辺ツールを個別に更新する

- [ ] TypeScriptの次期メジャー更新を調査する
- [ ] ESLintの次期メジャー更新を調査する
- [ ] Vitest、Prettier、Sharpの更新を個別に検証する

## 共通の検証

各変更で次を実行する。

```bash
pnpm test
pnpm lint
pnpm exec astro check
pnpm build
```

表示変更を含む場合は、Vercel Previewで最新記事、古いHTMLを含む記事、タグ、年別、旧URLリダイレクトを確認する。
